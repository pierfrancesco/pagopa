/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import MainContainer from './MainContainer';

storiesOf('MainContainer', module).add('default', () => <MainContainer />);
